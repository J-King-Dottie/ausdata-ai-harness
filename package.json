{
  "name": "abs-mcp-server",
  "version": "0.1.0",
  "description": "Nisaba unified MCP server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build:all": "npm run build",
    "start": "python -m backend.app.unified_mcp_server",
    "mcp:nisaba": "python -m backend.app.unified_mcp_server"
  }
}
